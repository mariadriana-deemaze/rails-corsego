- flash.each do |name, msg|
    - if msg.is_a?(String)
        %div{:class => "mb-5 px-4 py-2 border rounded-md #{name.to_s == 'notice' ? 'border-green-400 bg-green-100 text-green-500':'border-red-400 bg-red-100 text-red-500'}", :role => "alert"}
            %p 
                = msg
